// TODO includedBuild does not exist while running from root
task pluginBuild { dependsOn gradle.includedBuild('plugin').task('publishToMavenLocal') }
task toolsBuild { dependsOn gradle.includedBuild('tools').task('publishToMavenLocal') }

build.dependsOn(pluginBuild, toolsBuild)
